import { useCallback, useEffect, useRef, useState } from 'react';
import { ArrowLeft, ArrowRight, BookOpen, Check, ChevronDown, ChevronRight, Columns2, Rows2, FilePlus2, FileText, FolderOpen, Grid2X2, GripVertical, HelpCircle, LibraryBig, LoaderCircle, Maximize2, MoreHorizontal, PanelLeftClose, PanelLeftOpen, Plus, Search, Settings2, Sparkles, Star, Upload, X, Clock3, Pencil, Trash2, ExternalLink, Pin, PinOff, Minimize2, Undo2, Redo2, Mic } from 'lucide-react';
import type { Bootstrap, PaperDocument, Settings, TextSelection, Workspace } from '../shared/types';
import Library from './components/Library';
import RemovedArticles from './components/RemovedArticles';
import SettingsModal from './components/SettingsModal';
import AssistantPanel from './components/AssistantPanel';
import PdfPane from './components/PdfPane';
import useBackgroundIndex from './pdf/useBackgroundIndex';
import useReadingLayout from './hooks/useReadingLayout';
import useFloatingAssistant from './hooks/useFloatingAssistant';
import useDocumentEdits from './hooks/useDocumentEdits';
import { PLATFORM, PLATFORM_LABEL, shortcutLabel } from './platform';
import { I18nProvider, useI18n } from './i18n';
import { normalizeLanguage, translate, type Language } from '../shared/i18n';
import { UpdateNotice } from './components/UpdatePanel';

type Navigation = {page:number;nonce:number};
const errorText=(e:unknown)=>e instanceof Error?e.message.replace(/^Error invoking remote method '[^']+': (?:Error: )?/,''):String(e);

export default function App(){
  const [language,setLanguage]=useState<Language>(()=>{try{return normalizeLanguage(localStorage.getItem('folio:language'));}catch{return 'zh-CN';}});
  return <I18nProvider language={language}><FolioApp onLanguageChange={setLanguage}/></I18nProvider>;
}

function FolioApp({onLanguageChange}:{onLanguageChange:(language:Language)=>void}){
  const {t}=useI18n();
  const fileManager=PLATFORM==='darwin'?'Finder':PLATFORM==='win32'?t('文件资源管理器','File Explorer'):t('文件管理器','File manager');
  const [data,setData]=useState<Bootstrap|null>(null),[bootError,setBootError]=useState('');
  useEffect(()=>{
    if(!data)return;
    const next=normalizeLanguage(data.settings.language);onLanguageChange(next);
    document.documentElement.lang=next;
    try{localStorage.setItem('folio:language',next);}catch{/* Settings remain the source of truth when browser storage is unavailable. */}
  },[data?.settings.language,onLanguageChange]);
  const [activeId,setActiveId]=useState<string|null>(null),[filter,setFilter]=useState<'all'|'favorites'|'recent'|'removed'>('all');
  const [settingsOpen,setSettingsOpen]=useState(false);
  const [settingsSection,setSettingsSection]=useState<'updates'|undefined>();
  useEffect(()=>{if(!settingsOpen)setSettingsSection(undefined);},[settingsOpen]);
  const readingLayout=useReadingLayout();
  const sidebar=readingLayout.navigationExpanded,assistant=readingLayout.assistantOpen;
  const readerBody=useRef<HTMLDivElement>(null),assistantElement=useRef<HTMLDivElement>(null);
  const [readerWidth,setReaderWidth]=useState(0);
  const [busy,setBusy]=useState(false),[toast,setToast]=useState<{text:string;error:boolean}|null>(null);
  const [selection,setSelection]=useState<TextSelection|null>(null),[activePane,setActivePane]=useState<'left'|'right'>('left');
  const [nav,setNav]=useState<{left?:Navigation;right?:Navigation}>({});
  const [dragging,setDragging]=useState(false),[metadata,setMetadata]=useState(false),[docMenu,setDocMenu]=useState<string|null>(null);
  const [help,setHelp]=useState(false),[confirm,setConfirm]=useState<{title:string;description:string;action:()=>Promise<void>}|null>(null);
  const [rename,setRename]=useState<PaperDocument|null>(null);
  const [layoutMenu,setLayoutMenu]=useState(false);
  const [annotationHost,setAnnotationHost]=useState<HTMLElement|null>(null);
  const [voiceHost,setVoiceHost]=useState<HTMLElement|null>(null);
  const [voiceActive,setVoiceActive]=useState(false);
  const [voiceToggleRequest,setVoiceToggleRequest]=useState<{workspaceId:string;nonce:number}>();
  useEffect(()=>{
    if(!annotationHost)return;
    const stack=annotationHost.closest<HTMLElement>('.reader-stack');if(!stack)return;
    const observer=new ResizeObserver(entries=>stack.style.setProperty('--annotation-clearance',`${Math.ceil(entries[0].contentRect.height)+12}px`));
    observer.observe(annotationHost);return()=>{observer.disconnect();stack.style.removeProperty('--annotation-clearance');};
  },[annotationHost]);
  const [annotationTool,setAnnotationTool]=useState<{color:string;armed:boolean;kind?:'highlight'|'underline'|'strikeout'}>({color:'#f5ce65',armed:false,kind:'highlight'});
  const [askFocus,setAskFocus]=useState<{workspaceId:string;nonce:number}>();
  const paneContainer=useRef<HTMLDivElement>(null),dragDepth=useRef(0);
  const activeRef=useRef<Workspace|undefined>(undefined);
  const workspace=data?.workspaces.find(w=>w.id===activeId);activeRef.current=workspace;
  const inform=useCallback((text:string,error=false)=>setToast({text,error}),[]);
  const onError=useCallback((text:string)=>inform(text,true),[inform]);
  const update=useCallback((ws:Workspace)=>setData(previous=>previous?{...previous,workspaces:previous.workspaces.some(w=>w.id===ws.id)?previous.workspaces.map(w=>w.id===ws.id&&w.updatedAt<=ws.updatedAt?ws:w):[ws,...previous.workspaces]}:previous),[]);
  const documentEdits=useDocumentEdits(activeId??undefined,update,onError);
  const refresh=useCallback(async()=>{try{setData(await window.folio.bootstrap());}catch(e){onError(errorText(e));}},[onError]);
  useBackgroundIndex(workspace,async(id,doc,index)=>{update(await window.folio.indexDocument(id,doc,index));},onError);
  useEffect(()=>{if(!window.folio)return;return window.folio.onPrepareClose(async()=>{const pending:Promise<void>[]=[];window.dispatchEvent(new CustomEvent('folio:flush-notes',{detail:pending}));await Promise.all(pending);});},[]);
  const run=useCallback(async(action:()=>Promise<void>)=>{setBusy(true);try{await action();}catch(e){onError(errorText(e));}finally{setBusy(false);}},[onError]);
  const changeAnnotationToolbar=useCallback(async(annotationToolbar:'floating'|'fixed')=>{
    if(!data?.settings)return;
    try{const settings=await window.folio.saveSettings({...data.settings,annotationToolbar});setData(previous=>previous?{...previous,settings}:previous);}
    catch(e){onError(errorText(e));}
  },[data?.settings,onError]);
  useEffect(()=>{
    if(!window.folio){setBootError(t("请在 Folio 桌面应用中使用。开发运行：npm run dev","Open Folio as a desktop app. For development, run: npm run dev"));return;}
    void window.folio.bootstrap().then(setData).catch(e=>setBootError(errorText(e)));
    return window.folio.onOpen(ws=>{update(ws);setActiveId(ws.id);setFilter(value=>value==='removed'?'all':value);setSelection(null);});
  },[update]);
  useEffect(()=>{document.documentElement.dataset.theme=data?.settings.theme??'light';},[data?.settings.theme]);
  useEffect(()=>{if(!toast)return;const timer=setTimeout(()=>setToast(null),toast.error?12000:5000);return()=>clearTimeout(timer);},[toast]);
  useEffect(()=>{setSelection(null);setNav({});setDocMenu(null);setActivePane('left');readingLayout.exitFocus();setAnnotationTool(value=>({...value,armed:false}));},[activeId]);

  const patch=useCallback(async(id:string,value:Parameters<typeof window.folio.updateWorkspace>[1])=>{try{const ws=await window.folio.updateWorkspace(id,value);update(ws);return ws;}catch(e){onError(errorText(e));}},[update,onError]);
  const open=useCallback((id:string)=>{setActiveId(id);setFilter(value=>value==='removed'?'all':value);void patch(id,{lastReadAt:Date.now()});},[patch]);
  const create=useCallback((paths?:string[])=>void run(async()=>{const ws=await window.folio.createWorkspace(paths);if(ws){update(ws);open(ws.id);}}),[run,update,open]);
  const addDocuments=useCallback((paths?:string[])=>{const ws=activeRef.current;if(!ws)return;void run(async()=>{const next=await window.folio.importDocuments(ws.id,paths);if(next){update(next);inform(t("PDF 已加入当前文章工作区","PDF added to this workspace"));}});},[run,update,inform,t]);
  const toggleSplit=useCallback(()=>{const ws=activeRef.current;if(!ws)return;void patch(ws.id,{layout:{...ws.layout,split:!ws.layout.split,rightId:ws.layout.rightId||ws.documents.find(d=>d.id!==ws.layout.leftId)?.id||ws.layout.leftId}});},[patch]);
  const setLayout=useCallback((direction:'vertical'|'horizontal'|'none')=>{const ws=activeRef.current;if(!ws)return;setLayoutMenu(false);void patch(ws.id,{layout:{...ws.layout,split:direction!=='none',direction:direction==='none'?ws.layout.direction:direction,ratio:direction!==ws.layout.direction?50:ws.layout.ratio,rightId:ws.layout.rightId||ws.documents.find(d=>d.id!==ws.layout.leftId)?.id||ws.layout.leftId}});},[patch]);
  useEffect(()=>{if(!window.folio)return;return window.folio.onCommand(command=>{
    if(command==='updates'){setSettingsSection('updates');setSettingsOpen(true);}
    if(command==='settings')setSettingsOpen(true);if(command==='import')create();if(command==='supplement')addDocuments();if(command==='library'){setActiveId(null);setFilter('all');}if(command==='split')toggleSplit();if(command==='assistant')readingLayout.toggleAssistant();
    if(command==='focus-reading'&&activeRef.current)readingLayout.toggleFocus();
    if(command==='voice'&&activeRef.current&&PLATFORM==='darwin')setVoiceToggleRequest({workspaceId:activeRef.current.id,nonce:Date.now()});
    if(command==='undo'||command==='redo')documentEdits.undo(command);
    if(command==='split-vertical')setLayout('vertical');if(command==='split-horizontal')setLayout('horizontal');if(command==='split-none')setLayout('none');
    if(command==='export'&&activeRef.current)void run(async()=>{const result=await window.folio.exportMarkdown(activeRef.current!.id,'file');if(result)inform(t("笔记已导出：{v0}","Notes exported: {v0}",{v0:(result.path)}));});
  });},[create,addDocuments,toggleSplit,setLayout,run,inform,readingLayout.toggleAssistant,readingLayout.toggleFocus,documentEdits.undo,t]);
  useEffect(()=>{const handler=(e:KeyboardEvent)=>{if(e.key==='Escape'){setDocMenu(null);setHelp(false);}};window.addEventListener('keydown',handler);return()=>window.removeEventListener('keydown',handler);},[]);

  const navigate=(docId:string,page:number)=>{
    if(!workspace)return;
    const pane=workspace.layout.split&&workspace.layout.rightId===docId?'right':'left';
    if(pane==='left'&&workspace.layout.leftId!==docId)void patch(workspace.id,{layout:{...workspace.layout,leftId:docId}});
    setNav(v=>({...v,[pane]:{page,nonce:Date.now()}}));setActivePane(pane);
  };
  const splitFromPane=(side:'left'|'right',doc:PaperDocument,direction:'vertical'|'horizontal'|'none')=>{
    const ws=activeRef.current;if(!ws)return;
    setActivePane(side);setLayoutMenu(false);
    if(direction!=='none'&&!ws.layout.split){
      void patch(ws.id,{layout:{...ws.layout,split:true,direction,leftId:doc.id,rightId:doc.id,ratio:50}});
    }else setLayout(direction);
  };
  const drop=(event:React.DragEvent)=>{
    event.preventDefault();setDragging(false);dragDepth.current=0;
    const paths=Array.from(event.dataTransfer.files).map(file=>window.folio.pathForFile(file)).filter(Boolean);
    if(!paths.length)return;if(workspace)addDocuments(paths);else create(paths);
  };
  const resize=(event:React.PointerEvent)=>{
    event.preventDefault();const area=paneContainer.current;if(!area||!workspace)return;
    const ws=workspace;let ratio=ws.layout.ratio;
    const horizontal=ws.layout.direction==='horizontal';
    const move=(e:PointerEvent)=>{const rect=area.getBoundingClientRect();ratio=Math.max(28,Math.min(72,(horizontal?(e.clientY-rect.top)/rect.height:(e.clientX-rect.left)/rect.width)*100));area.style.setProperty('--split-ratio',`${ratio}%`);};
    const finish=()=>{document.body.classList.remove('resizing');window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',finish);void patch(ws.id,{layout:{...ws.layout,ratio}});};
    document.body.classList.add('resizing');window.addEventListener('pointermove',move);window.addEventListener('pointerup',finish,{once:true});
  };
  useEffect(()=>{
    const element=readerBody.current;if(!element)return;
    const observer=new ResizeObserver(entries=>setReaderWidth(Math.round(entries[0].contentRect.width)));
    observer.observe(element);return()=>observer.disconnect();
  },[activeId]);
  const resizeAssistant=(event:React.PointerEvent)=>{
    event.preventDefault();const element=assistantElement.current;if(!element)return;
    const startX=event.clientX,startWidth=element.getBoundingClientRect().width;
    let width=startWidth;
    const move=(pointer:PointerEvent)=>{width=Math.max(320,Math.min(600,startWidth+startX-pointer.clientX));element.style.setProperty('--assistant-width',`${width}px`);};
    const finish=()=>{document.body.classList.remove('resizing-assistant');window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',finish);window.removeEventListener('pointercancel',finish);readingLayout.resizeAssistant(width);};
    document.body.classList.add('resizing-assistant');window.addEventListener('pointermove',move);window.addEventListener('pointerup',finish,{once:true});window.addEventListener('pointercancel',finish,{once:true});
  };
  const deleteWorkspace=(ws:Workspace)=>setConfirm({title:t("移除这篇论文？","Remove this paper?"),description:t("PDF、笔记、批注和对话会一起保留在「已移除文章」中，不会自动清理。你可以随时从那里恢复。","The PDFs, notes, annotations, and conversations will stay in Removed Papers until you permanently delete them. You can restore them at any time."),action:async()=>{await window.folio.deleteWorkspace(ws.id);setData(await window.folio.bootstrap());if(activeId===ws.id)setActiveId(null);inform(t("论文已移至「已移除文章」，可随时恢复","Paper moved to Removed Papers. You can restore it at any time."));}});
  const restoreRemoved=async(id:string)=>{try{await window.folio.restoreWorkspace(id);setData(await window.folio.bootstrap());inform(t("论文已恢复到文献库，PDF 与阅读记录均已保留","Paper restored with its PDFs and reading history"));}catch(e){onError(errorText(e));}};
  const purgeRemoved=async(id:string)=>{try{if(await window.folio.purgeWorkspace(id)){setData(await window.folio.bootstrap());inform(t("已彻底删除这篇论文及其工作区文件","Paper and workspace files permanently deleted"));}}catch(e){onError(errorText(e));}};
  const removeDoc=(doc:PaperDocument)=>{if(!workspace)return;const ws=workspace;setDocMenu(null);setConfirm({title:t("移除此 PDF？","Remove this PDF?"),description:t("文件会移到文章文件夹内的 .removed 目录，保留原件。此文件的阅读批注不再显示在当前工作区。","The file will be kept in the paper folder's .removed directory. Its annotations will no longer appear in this workspace."),action:async()=>{update(await window.folio.removeDocument(ws.id,doc.id));await documentEdits.refresh(ws.id);}});};

  const horizontal=workspace?.layout.direction==='horizontal';
  const assistantDocked=readingLayout.preferences.assistantMode==='docked'&&readerWidth-readingLayout.preferences.assistantWidth>=(workspace?.layout.split&&!horizontal?840:460);
  const floatingAssistant=useFloatingAssistant({container:readerBody,panel:assistantElement,enabled:Boolean(workspace)&&assistant&&!assistantDocked,workspaceId:workspace?.id,position:readingLayout.preferences.assistantPosition,onMove:readingLayout.moveAssistant,onResize:readingLayout.resizeAssistant});

  if(bootError)return <div className="boot-screen"><BookOpen size={44}/><h1>Folio</h1><p>{bootError}</p><button onClick={()=>location.reload()}>{t("重新加载","Reload")}</button></div>;
  if(!data)return <div className="boot-screen"><div className="brand-mark"><BookOpen size={30}/></div><h1>Folio</h1><LoaderCircle className="spin" size={20}/><p>{t("正在打开你的阅读空间…","Opening your library…")}</p></div>;

  const left=workspace?.documents.find(d=>d.id===workspace.layout.leftId)??workspace?.documents[0];
  const right=workspace?.documents.find(d=>d.id===workspace.layout.rightId)??workspace?.documents[1]??left;
  const paneLabel=(side:'left'|'right')=>horizontal?(side==='left'?t("上方","Top"):t("下方","Bottom")):(side==='left'?t("左侧","Left"):t("右侧","Right"));
  const renderPane=(side:'left'|'right',doc:PaperDocument|undefined)=>doc&&workspace?<section className={`reading-column ${activePane===side?'is-active':''}`} aria-label={t("{v0}阅读区","{v0} reading pane",{v0:(paneLabel(side))})} onPointerDownCapture={()=>setActivePane(side)}>
    <header className="document-tab"><span className={`document-role ${doc.role}`}>{doc.role==='main'?t("正文","Main"):t("补充","Supplement")}</span><FileText size={14}/><select aria-label={`${paneLabel(side)} PDF`} value={doc.id} onChange={e=>void patch(workspace.id,{layout:{...workspace.layout,[side==='left'?'leftId':'rightId']:e.target.value}})}>{workspace.documents.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><ChevronDown size={12}/>{side==='right'&&<button className="icon-button" title={t("关闭第二阅读区","Close second pane")} onClick={toggleSplit}><X size={14}/></button>}</header>
    <PdfPane key={`${workspace.id}-${side}-${doc.id}`} workspaceId={workspace.id} document={{...doc,view:workspace.layout.views?.[side]?.documentId===doc.id?workspace.layout.views[side]!.state:doc.view}} readingTheme={data.settings.readingTheme} editBusy={documentEdits.busy} annotationToolbar={data.settings.annotationToolbar} annotationToolbarHost={annotationHost} annotationTool={annotationTool} onAnnotationToolChange={value=>setAnnotationTool(previous=>({...previous,...value}))} onAnnotationToolbarChange={mode=>void changeAnnotationToolbar(mode)} active={activePane===side} onActivate={()=>setActivePane(side)} onDocumentPatch={async value=>{try{if(value.view)update(await window.folio.updateView(workspace.id,doc.id,side,value.view));const rest={...value};delete rest.view;if(Object.keys(rest).length)await documentEdits.editDocument(workspace.id,doc.id,rest);}catch(e){onError(errorText(e));throw e;}}} onIndexed={index=>void window.folio.indexDocument(workspace.id,doc.id,index).then(update).catch(e=>onError(errorText(e)))} onSelection={setSelection} onAsk={value=>{setSelection(value);readingLayout.openAssistant();setAskFocus({workspaceId:workspace.id,nonce:Date.now()});}} onError={onError} navigation={nav[side]} onSplit={direction=>splitFromPane(side,doc,direction)}/>
  </section>:<div className="no-pdf"><FileText size={35}/><h3>{t("给这篇论文添加 PDF","Add a PDF to this paper")}</h3><p>{t("导入的旧笔记已保留，可以在这里继续阅读。","Your imported notes are saved. Add a PDF to continue reading.")}</p><button className="primary-button" onClick={()=>addDocuments()}><Plus size={15}/>{t(" 添加 PDF"," Add PDF")}</button></div>;

  return <div className={`folio-app ${readingLayout.focused&&workspace?'focus-reading':''}`} data-platform={PLATFORM} onDragEnter={e=>{if(e.dataTransfer.types.includes('Files')){e.preventDefault();dragDepth.current++;setDragging(true);}}} onDragOver={e=>{if(e.dataTransfer.types.includes('Files'))e.preventDefault();}} onDragLeave={e=>{e.preventDefault();if(--dragDepth.current<=0){dragDepth.current=0;setDragging(false);}}} onDrop={drop}>
    <div className="titlebar"><div className="traffic-space"/><span className="titlebar-label">Folio <span>/</span> {workspace?t("阅读工作区","Workspace"):filter==='removed'?t("已移除文章","Removed Papers"):t("个人文献库","Library")}</span><div className="titlebar-right"><span className="local-dot"/>{t(" 本地保存 "," Saved locally ")}<span className="titlebar-separator"/>{t(" 为专注而留白"," Room to focus")}</div></div>
    <UpdateNotice/>
    <div className="app-body">
      {(!workspace||sidebar)&&<aside className={`app-sidebar ${workspace?'in-workspace':''}`}>
        <button className="brand" onClick={()=>{setActiveId(null);setFilter('all');}} title={t("返回论文库","Back to library")}><span className="brand-mark"><BookOpen size={21} strokeWidth={1.6}/></span><strong>folio<span>{t("阅读，亦是思考。","A place to think.")}</span></strong></button>
        <div className="sidebar-section-label">{t("你的阅读空间","YOUR LIBRARY")}</div>
        <nav className="main-navigation">{[{id:'all' as const,label:t("全部论文","All Papers"),icon:LibraryBig,count:data.workspaces.length},{id:'recent' as const,label:t("最近阅读","Recently Read"),icon:Clock3},{id:'favorites' as const,label:t("我的收藏","Favorites"),icon:Star,count:data.workspaces.filter(w=>w.favorite).length},{id:'removed' as const,label:t("已移除文章","Removed Papers"),icon:Trash2,count:data.removedWorkspaces.length}].map(item=><button key={item.id} className={!workspace&&filter===item.id?'selected':''} onClick={()=>{setActiveId(null);setFilter(item.id);if(item.id==='removed')void refresh();}}><item.icon size={17}/><span>{item.label}</span>{item.count!==undefined&&<small>{item.count}</small>}</button>)}</nav>
        {workspace?<><div className="sidebar-rule"/><div className="sidebar-section-label">{t("当前文章 ","CURRENT PAPER ")}<button className="icon-button" title={t("在 {v0} 中打开文件夹","Open folder in {v0}",{v0:(fileManager)})} onClick={()=>void window.folio.revealWorkspace(workspace.id).catch(e=>onError(errorText(e)))}><FolderOpen size={14}/></button></div><button className="workspace-name" title={t("编辑论文信息","Edit paper details")} onClick={()=>setMetadata(true)}>{workspace.title}<Pencil size={12}/></button><div className="workspace-files">{workspace.documents.map(doc=><div className={`workspace-file ${left?.id===doc.id||workspace.layout.split&&right?.id===doc.id?'selected':''}`} key={doc.id}><button className="file-main" title={t("在{v0}侧打开 {v1}","Open {v1} in the {v0} pane",{v0:(activePane==='left'?t('左','left'):t('右','right')),v1:(doc.name)})} onClick={()=>void patch(workspace.id,{layout:{...workspace.layout,[activePane==='right'&&workspace.layout.split?'rightId':'leftId']:doc.id}})}><span className={`file-icon ${doc.role}`}><FileText size={17}/></span><span><strong>{doc.name}</strong><small>{doc.role==='main'?t("正文","Main"):t("补充材料","Supplement")} · {doc.pageCount?t("{v0} 页","{v0} pages",{v0:(doc.pageCount)}):t("待读取","Not indexed")}</small></span></button><button className="icon-button file-more" title={t("{v0} 选项","Options for {v0}",{v0:(doc.name)})} onClick={()=>setDocMenu(docMenu===doc.id?null:doc.id)}><MoreHorizontal size={14}/></button>{docMenu===doc.id&&<><div className="menu-dismiss" onClick={()=>setDocMenu(null)}/><div className="file-menu"><button onClick={()=>{setRename(doc);setDocMenu(null);}}><Pencil size={13}/>{t(" 重命名"," Rename")}</button><button onClick={()=>{setDocMenu(null);void window.folio.updateDocument(workspace.id,doc.id,{role:'main'}).then(update).catch(e=>onError(errorText(e)));}}><BookOpen size={13}/>{t(" 设为正文"," Set as main paper")}</button><button onClick={()=>{setDocMenu(null);void patch(workspace.id,{layout:{...workspace.layout,rightId:doc.id,split:true}});}}><Columns2 size={13}/>{t(" 在第二阅读区打开"," Open in second pane")}</button><button className="danger" onClick={()=>removeDoc(doc)}><Trash2 size={13}/>{t(" 移除文件"," Remove file")}</button></div></>}</div>)}</div><button className="add-attachment" onClick={()=>addDocuments()} disabled={busy}><Plus size={15}/>{t(" 添加补充材料"," Add supplement")}</button><div className="sidebar-paper-tags">{workspace.tags.map(t=><span key={t}>#{t}</span>)}</div></>:<div className="sidebar-note"><span>THE READING ROOM</span><p>{t("让论文成为知识，","Read. Connect.")}<br/>{t("让灵感有处安放。","Make room for ideas.")}</p><div className="sidebar-note-line"/></div>}
        <div className="sidebar-bottom"><button onClick={()=>setHelp(true)}><HelpCircle size={16}/><span>{t("阅读小贴士","Reading tips")}</span><kbd>?</kbd></button><button onClick={()=>setSettingsOpen(true)}><Settings2 size={16}/><span>{t("设置与连接","Settings")}</span><kbd>{shortcutLabel(',')}</kbd></button><div className="local-profile"><span className="profile-avatar">F</span><div><strong>{t("我的本地空间","Local workspace")}</strong><small>Folio {data.version} · {PLATFORM_LABEL}</small></div><span className="local-dot"/></div></div>
      </aside>}
      {workspace&&!sidebar&&!readingLayout.focused&&<aside className="app-rail" aria-label={t("精简导航","Compact navigation")}><button className="rail-brand" aria-label={t("返回文献库","Back to library")} title={t("返回文献库","Back to library")} onClick={()=>setActiveId(null)}><BookOpen size={21}/></button><button aria-label={t("文章与补充材料","Paper and supplements")} title={t("文章与补充材料","Paper and supplements")} onClick={readingLayout.toggleNavigation}><FolderOpen size={19}/></button><button aria-label={t("全部论文","All Papers")} title={t("全部论文","All Papers")} onClick={()=>{setActiveId(null);setFilter('all');}}><LibraryBig size={18}/></button><button aria-label={t("已移除文章","Removed Papers")} title={t("已移除文章","Removed Papers")} onClick={()=>{setActiveId(null);setFilter('removed');}}><Trash2 size={18}/></button><span className="rail-spacer"/><button aria-label={t("阅读小贴士","Reading tips")} title={t("阅读小贴士","Reading tips")} onClick={()=>setHelp(true)}><HelpCircle size={18}/></button><button aria-label={t("设置与连接","Settings")} title={t("设置与连接","Settings")} onClick={()=>setSettingsOpen(true)}><Settings2 size={18}/></button></aside>}
      <main className={`app-main ${workspace?'reader-main':'library-main'}`}>
        {workspace?<><header className="workspace-topbar"><button className="icon-button" aria-label={sidebar?t("收起文章导航","Collapse navigation"):t("展开文章导航","Expand navigation")} title={sidebar?t("收起文章导航","Collapse navigation"):t("展开文章导航","Expand navigation")} onClick={readingLayout.toggleNavigation}>{sidebar?<PanelLeftClose size={17}/>:<PanelLeftOpen size={17}/>}</button><button className="back-button" onClick={()=>setActiveId(null)}><ArrowLeft size={15}/><span>{t("文献库","Library")}</span></button><span className="breadcrumb-separator">/</span><button className="workspace-top-title" onClick={()=>setMetadata(true)} title={t("编辑论文信息","Edit paper details")}>{workspace.title}</button><div className="workspace-top-actions"><button className="icon-button" aria-label={t("撤销上一步","Undo")} title={t("撤销批注或目录编辑 · {v0}","Undo annotation or outline edit · {v0}",{v0:(shortcutLabel('Z'))})} disabled={!documentEdits.canUndo} onMouseDown={event=>event.preventDefault()} onClick={()=>documentEdits.undo('undo',true)}><Undo2 size={16}/></button><button className="icon-button" aria-label={t("重做上一步","Redo")} title={t("重做 · {v0}","Redo · {v0}",{v0:(shortcutLabel('⇧ Z'))})} disabled={!documentEdits.canRedo} onMouseDown={event=>event.preventDefault()} onClick={()=>documentEdits.undo('redo',true)}><Redo2 size={16}/></button><button className={`icon-button ${workspace.favorite?'favorite':''}`} title={workspace.favorite?t("取消收藏","Remove from favorites"):t("收藏论文","Add to favorites")} onClick={()=>void patch(workspace.id,{favorite:!workspace.favorite})}><Star size={16} fill={workspace.favorite?'currentColor':'none'}/></button><button className="top-action" title={t("添加补充材料","Add supplement")} onClick={()=>addDocuments()} disabled={busy}>{busy?<LoaderCircle size={15} className="spin"/>:<FilePlus2 size={15}/>}<span>{t("添加 PDF","Add PDF")}</span></button><span className="action-divider"/><div className="layout-control"><button className={`top-action ${workspace.layout.split?'is-selected':''}`} aria-label={t("阅读布局","Reading layout")} title={t("选择阅读布局","Choose reading layout")} aria-expanded={layoutMenu} onClick={()=>setLayoutMenu(v=>!v)}>{horizontal&&workspace.layout.split?<Rows2 size={16}/>:<Columns2 size={16}/>}<span>{workspace.layout.split?(horizontal?t("上下分割","Stacked"):t("左右分割","Side by side")):t("单栏","Single pane")}</span><ChevronDown size={10}/></button>{layoutMenu&&<><div className="menu-dismiss" onClick={()=>setLayoutMenu(false)}/><div className="layout-menu" role="menu"><span>{t("阅读布局","Reading layout")}</span><button role="menuitemradio" aria-checked={!workspace.layout.split} onClick={()=>setLayout('none')}><FileText size={16}/><span>{t("单栏阅读","Single pane")}<small>{t("专注当前文档","Read one document")}</small></span>{!workspace.layout.split&&<Check size={14}/>}</button><button role="menuitemradio" aria-checked={workspace.layout.split&&!horizontal} onClick={()=>setLayout('vertical')}><Columns2 size={16}/><span>{t("纵向分割 · 左右","Side by side")}<small>{t("正文与补充材料并排对照","Compare the paper and supplements")}</small></span>{workspace.layout.split&&!horizontal&&<Check size={14}/>}</button><button role="menuitemradio" aria-checked={workspace.layout.split&&horizontal} onClick={()=>setLayout('horizontal')}><Rows2 size={16}/><span>{t("横向分割 · 上下","Stacked")}<small>{t("保留页面宽度，比较图表与段落","Keep full page width for figures and text")}</small></span>{workspace.layout.split&&horizontal&&<Check size={14}/>}</button><footer>{t("每个区域都可独立打开同一或不同 PDF","Each pane can show the same or a different PDF")}</footer></div></>}</div><button className={`top-action focus-toggle ${readingLayout.focused?'is-selected':''}`} aria-label={readingLayout.focused?t("退出专注阅读","Leave focus mode"):t("专注阅读","Focus mode")} title={t("专注阅读 · {v0}","Focus mode · {v0}",{v0:(shortcutLabel('⇧ F'))})} aria-pressed={readingLayout.focused} onClick={readingLayout.toggleFocus}>{readingLayout.focused?<Minimize2 size={16}/>:<Maximize2 size={16}/>}<span>{readingLayout.focused?t("退出专注","Leave focus"):t("专注阅读","Focus mode")}</span></button>{PLATFORM==='darwin'&&<button className={`top-action voice-toggle ${voiceActive?'is-selected':''}`} aria-label={t("语音对话","Voice conversation")} aria-pressed={voiceActive} title={t("Apple 语音对话","Apple voice conversation")} onClick={()=>setVoiceToggleRequest({workspaceId:workspace.id,nonce:Date.now()})}><Mic size={16}/><span>{t("语音","Voice")}</span></button>}<button className={`top-action ai-toggle ${assistant?'is-selected':''}`} aria-label={t("阅读伙伴","Reading companion")} aria-expanded={assistant} title={t("显示 / 隐藏 AI · {v0}","Show / hide AI · {v0}",{v0:(shortcutLabel('J'))})} onClick={readingLayout.toggleAssistant}><Sparkles size={16}/><span>{t("阅读伙伴","Reading companion")}</span></button></div></header><div className="reader-body" ref={readerBody}><div className="reader-stack"><div ref={setAnnotationHost} role="group" aria-label={t("共用批注工具栏","Shared annotation toolbar")} data-active-pane={activePane} className={`shared-annotation-host ${data.settings.annotationToolbar??'floating'}`}/><div className={`pdf-columns ${workspace.layout.split?'split':''} ${horizontal?'horizontal':''}`} style={{'--split-ratio':`${workspace.layout.ratio}%`} as React.CSSProperties} ref={paneContainer}>{renderPane('left',left)}{workspace.layout.split&&<><div className="split-divider" role="separator" aria-label={t("调整分栏宽度","Resize PDF panes")} aria-orientation={horizontal?"horizontal":"vertical"} tabIndex={0} onPointerDown={resize} onKeyDown={e=>{if((horizontal?['ArrowUp','ArrowDown']:['ArrowLeft','ArrowRight']).includes(e.key)){e.preventDefault();void patch(workspace.id,{layout:{...workspace.layout,ratio:Math.max(28,Math.min(72,workspace.layout.ratio+(['ArrowRight','ArrowDown'].includes(e.key)?2:-2)))}});}}}><GripVertical size={12}/></div>{renderPane('right',right)}</>}</div><footer className="reader-status"><span><span className="local-dot"/>{t(" 阅读进度与笔记保存在本机"," Reading progress and notes are saved locally")}</span><button onClick={()=>void window.folio.revealWorkspace(workspace.id).catch(e=>onError(errorText(e)))}><FolderOpen size={11}/>{t(" 文章文件夹 "," Paper folder ")}<ExternalLink size={10}/></button><span>{workspace.documents.length}{t(" 个 PDF · "," PDFs · ")}{workspace.documents.reduce((n,d)=>n+d.annotations.length,0)}{t(" 条批注"," annotations")}</span></footer></div><div className="folio-voice-host" ref={setVoiceHost}/>{<div ref={assistantElement} className={`assistant-shell ${assistantDocked?'is-docked':'is-overlay'}`} data-open={assistant} style={{display:assistant?undefined:'none','--assistant-width':`${readingLayout.preferences.assistantWidth}px`} as React.CSSProperties}><div className="assistant-resize-handle" role="separator" aria-label={t("调整阅读伙伴宽度","Resize reading companion")} aria-orientation="vertical" aria-valuemin={320} aria-valuemax={600} aria-valuenow={readingLayout.preferences.assistantWidth} tabIndex={0} onPointerDown={assistantDocked?resizeAssistant:floatingAssistant.resize} onKeyDown={event=>{if(event.key==='ArrowLeft'||event.key==='ArrowRight'){event.preventDefault();readingLayout.resizeAssistant(readingLayout.preferences.assistantWidth+(event.key==='ArrowLeft'?20:-20));}}}/>{readingLayout.preferences.assistantMode==='docked'&&!assistantDocked&&<div className="assistant-space-notice">{t("窗口较窄，阅读伙伴暂时浮出显示","The companion floats while the window is narrow")}</div>}<AssistantPanel key={workspace.id} voiceHost={voiceHost} voiceToggleRequest={voiceToggleRequest?.workspaceId===workspace.id?voiceToggleRequest:undefined} onVoiceActiveChange={setVoiceActive} workspace={workspace} settings={data.settings} selection={selection} focusRequest={askFocus} dragHandleProps={floatingAssistant.dragHandleProps} panelControls={<><button className="fl-icon-button" aria-label={readingLayout.preferences.assistantMode==='docked'?t("取消固定阅读伙伴","Unpin reading companion"):t("固定阅读伙伴","Pin reading companion")} title={readingLayout.preferences.assistantMode==='docked'?t("改为浮出显示","Show as floating panel"):t("固定在右侧","Dock on the right")} aria-pressed={readingLayout.preferences.assistantMode==='docked'} onClick={readingLayout.toggleDocked}>{readingLayout.preferences.assistantMode==='docked'?<PinOff size={15}/>:<Pin size={15}/>}</button><button className="fl-icon-button" aria-label={t("关闭阅读伙伴","Close reading companion")} title={t("关闭阅读伙伴","Close reading companion")} onClick={readingLayout.closeAssistant}><X size={16}/></button></>} onClearSelection={()=>setSelection(null)} onWorkspace={update} onSettings={()=>setSettingsOpen(true)} onError={onError} onNavigate={navigate}/></div>}</div></>:<><div className="library-topbar"><div><span className="tiny-app-icon"><Grid2X2 size={14}/></span> LIBRARY <span>/</span> YOUR PERSONAL COLLECTION</div><button onClick={()=>setSettingsOpen(true)}><span className={`connection-dot ${data.settings.providers[data.settings.activeProvider].hasKey?'connected':''}`}/>{data.settings.providers[data.settings.activeProvider].hasKey?t("AI 已连接","AI configured"):t("连接 DeepSeek","Connect DeepSeek")}<ArrowRight size={13}/></button></div>{filter==='removed'?<RemovedArticles items={data.removedWorkspaces} onRestore={restoreRemoved} onPurge={purgeRemoved}/>:<Library workspaces={data.workspaces} onOpen={open} onCreate={()=>create()} onDemo={()=>void run(async()=>{const ws=await window.folio.createDemo();update(ws);open(ws.id);})} onToggleFavorite={ws=>void patch(ws.id,{favorite:!ws.favorite})} onDelete={deleteWorkspace} filter={filter} busy={busy}/>}</>}
      </main>
    </div>
    {dragging&&<div className="drop-overlay"><div><span><Upload size={32}/></span><h2>{workspace?t("加入这篇论文的工作区","Add to this workspace"):t("开始一篇新的阅读","Start a new paper")}</h2><p>{t("松开鼠标，导入 PDF 或文章文件夹","Drop PDFs or a paper folder to import")}</p><small>{t("正文和补充材料可以一起拖入","You can drop the paper and supplements together")}</small></div></div>}
    {settingsOpen&&<SettingsModal initialSection={settingsSection} settings={data.settings} onClose={()=>setSettingsOpen(false)} onSaved={(settings:Settings)=>{setData(d=>d?{...d,settings}:d);inform(translate(settings.language,"设置已保存","Settings saved"));}} onRefresh={()=>void refresh()} onError={onError}/>}
    {metadata&&workspace&&<MetadataModal workspace={workspace} onClose={()=>setMetadata(false)} onSave={async value=>{await patch(workspace.id,value);setMetadata(false);}}/>}
    {rename&&workspace&&<SimpleModal title={t("重命名 PDF","Rename PDF")} onClose={()=>setRename(null)}><form onSubmit={e=>{e.preventDefault();const name=String(new FormData(e.currentTarget).get('name')??'').trim();if(name)void run(async()=>{update(await window.folio.updateDocument(workspace.id,rename.id,{name}));setRename(null);});}}><label>{t("显示名称","Display name")}<input name="name" defaultValue={rename.name} autoFocus required/></label><div className="modal-actions"><button type="button" onClick={()=>setRename(null)}>{t("取消","Cancel")}</button><button className="primary-button" type="submit">{t("保存","Save")}</button></div></form></SimpleModal>}
    {confirm&&<SimpleModal title={confirm.title} onClose={()=>setConfirm(null)}><p>{confirm.description}</p><div className="modal-actions"><button onClick={()=>setConfirm(null)}>{t("取消","Cancel")}</button><button className="danger-button" disabled={busy} onClick={()=>void run(async()=>{await confirm.action();setConfirm(null);})}>{busy?t("处理中…","Working…"):t("确认移除","Remove")}</button></div></SimpleModal>}
    {help&&<SimpleModal title={t("给阅读留一点空间","Reading tips")} onClose={()=>setHelp(false)}><div className="help-items"><p><Columns2 size={20}/><span><strong>{t("让正文与证据并排","Read the paper alongside its evidence")}</strong>{t("选择左右或上下分割，在两个阅读区分别选择 PDF。也可以打开同一文件。拖动分隔线调整比例。","Choose side-by-side or stacked panes. Open different PDFs, or two views of the same file. Drag the divider to resize them.")}</span></p><p><Pencil size={20}/><span><strong>{t("在纸页上留下想法","Make notes as you read")}</strong>{t("选中文字后可从工具栏或右键菜单添加高亮、下划线、删除线；撤销和重做也适用于目录编辑。","Select text to highlight, underline, or strike through it using the toolbar or context menu. Undo and redo also work for outline edits.")}</span></p><p><Sparkles size={20}/><span><strong>{t("从一个好问题开始","Ask a question")}</strong>{t("阅读伙伴默认浮出，不挤压 PDF；可固定到右侧。专注阅读会暂时收起导航与助手，并保留草稿。设置中填写自己的 API Key 后即可提问。","The reading companion floats over the page or docks on the right. Focus mode hides navigation and the companion while keeping your draft. Add your own API key in Settings to use AI.")}</span></p><p><FolderOpen size={20}/><span><strong>{t("把笔记带回自己的知识库","Keep your notes in your own library")}</strong>{t("选择 Obsidian 仓库，在「笔记」中导出。设置中的完整备份包含 PDF 和所有阅读记录。","Choose an Obsidian vault and export from Notes. A full backup in Settings includes PDFs and all reading records.")}</span></p></div><div className="shortcut-list"><span>{t("导入论文 ","Import paper ")}<kbd>{shortcutLabel('O')}</kbd></span><span>{t("文档搜索 ","Search PDF ")}<kbd>{shortcutLabel('F')}</kbd></span><span>{t("切换助手 ","Toggle companion ")}<kbd>{shortcutLabel('J')}</kbd></span><span>{t("专注阅读 ","Focus mode ")}<kbd>{shortcutLabel('⇧ F')}</kbd></span><span>{t("撤销编辑 ","Undo edit ")}<kbd>{shortcutLabel('Z')}</kbd></span><span>{t("重做编辑 ","Redo edit ")}<kbd>{shortcutLabel('⇧ Z')}</kbd></span><span>{t("切换分栏 ","Toggle split ")}<kbd>{shortcutLabel('\\')}</kbd></span></div></SimpleModal>}
    {toast&&<div className={`app-toast ${toast.error?'error':''}`} role={toast.error?'alert':'status'}>{toast.error?<HelpCircle size={17}/>:<Check size={17}/>}<span>{toast.text}</span><button onClick={()=>setToast(null)} aria-label={t("关闭通知","Dismiss notification")}><X size={15}/></button></div>}
  </div>;
}

function SimpleModal({title,onClose,children}:{title:string;onClose:()=>void;children:React.ReactNode}){
  const {t}=useI18n();
  const ref=useRef<HTMLDivElement>(null);
  useEffect(()=>{const handler=(e:KeyboardEvent)=>{if(e.key==='Escape'){e.stopPropagation();onClose();}};window.addEventListener('keydown',handler);ref.current?.focus();return()=>window.removeEventListener('keydown',handler);},[onClose]);
  return <div className="modal-overlay" onMouseDown={e=>{if(e.target===e.currentTarget)onClose();}}><div ref={ref} className="simple-modal" role="dialog" aria-modal="true" aria-label={title} tabIndex={-1}><header><h2>{title}</h2><button className="icon-button" title={t("关闭","Close")} onClick={onClose}><X size={18}/></button></header>{children}</div></div>;
}
function MetadataModal({workspace,onClose,onSave}:{workspace:Workspace;onClose:()=>void;onSave:(value:Partial<Workspace>)=>Promise<void>}){
  const {t}=useI18n();
  const [saving,setSaving]=useState(false);
  return <SimpleModal title={t("论文信息","Paper details")} onClose={onClose}><form onSubmit={e=>{e.preventDefault();const data=new FormData(e.currentTarget);setSaving(true);void onSave({title:String(data.get('title')).trim(),authors:String(data.get('authors')),journal:String(data.get('journal')),doi:String(data.get('doi')).trim(),tags:String(data.get('tags')).split(/[,，]/).map(t=>t.trim()).filter(Boolean)}).finally(()=>setSaving(false));}}><label>{t("论文标题","Title")}<input name="title" defaultValue={workspace.title} required autoFocus/></label><label>{t("作者","Authors")}<input name="authors" defaultValue={workspace.authors} placeholder={t("作者姓名","Author names")}/></label><div className="form-row"><label>{t("期刊","Journal")}<input name="journal" defaultValue={workspace.journal}/></label><label>DOI<input name="doi" defaultValue={workspace.doi} placeholder="10.xxxx/…"/></label></div><label>{t("研究标签","Tags")}<input name="tags" defaultValue={workspace.tags.join(', ')} placeholder={t("用逗号分隔，用于检索与跨文章记忆召回","Separate tags with commas; used for search and related-paper memory")}/></label><div className="modal-actions"><button type="button" onClick={onClose}>{t("取消","Cancel")}</button><button type="submit" className="primary-button" disabled={saving}>{saving?t("保存中…","Saving…"):t("保存信息","Save details")}</button></div></form></SimpleModal>;
}
